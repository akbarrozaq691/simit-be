APP_PORT=8888

POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=simit
POSTGRES_USER=simit
POSTGRES_PASSWORD=changeme

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@simit.local

# S3-compatible cloud storage (AWS S3, MinIO, Cloudflare R2, etc.)
# Leave empty to disable uploads. Uploads will fail with a clear error until credentials are set.
STORAGE_BASE_URL=
STORAGE_BUCKET=
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=
STORAGE_REGION=auto
